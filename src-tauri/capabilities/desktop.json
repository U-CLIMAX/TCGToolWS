{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "enables desktop-specific permissions",
  "windows": ["main"],
  "platforms": ["windows", "macOS", "linux"],
  "permissions": ["window-state:default"]
}
